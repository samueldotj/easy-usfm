{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is permitted to do.",
  "windows": ["main"],
  "permissions": ["core:default"],
  "comment": [
    "SECURITY: capabilities are granted as they become necessary, never in",
    "advance. `core:default` is the minimum a window needs to exist. File",
    "access arrives with P1.10 and is scoped to files the user chose through",
    "a dialog -- not to a directory, and not to a glob."
  ]
}
